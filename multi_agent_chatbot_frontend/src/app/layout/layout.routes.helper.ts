/**
 * Utility to generate commands for activating the sidebar outlet with agents list.
 * Not strictly necessary but can be used in future navigation actions.
 */
export const sidebarAgentsOutlet = () => ({ outlets: { sidebar: ['agents'] } });
